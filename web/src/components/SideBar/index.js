import { SideBarContainer, SideBarWraper } from "./styled";

const SideBar = () => {
  return (
    <SideBarContainer>
      <SideBarWraper />
    </SideBarContainer>
  );
};

export default SideBar;
